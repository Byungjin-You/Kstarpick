# 무한 새로고침 문제 디버깅 가이드

## 🔍 문제 확인 방법

최초 웹사이트 진입 시 무한 새로고침이 발생하는 문제를 해결하기 위해 다음 단계를 따라주세요.

### 1단계: 브라우저 콘솔 확인

1. **Chrome/Edge 개발자 도구 열기**
   - Windows: `F12` 또는 `Ctrl + Shift + I`
   - Mac: `Cmd + Option + I`

2. **Console 탭으로 이동**

3. **웹사이트 접속** (http://localhost:3000)

4. **에러 메시지 확인**
   - 빨간색 에러 메시지가 있는지 확인
   - 스크린샷을 찍어두세요

### 2단계: Network 탭 확인

1. **Network 탭**으로 이동
2. **Preserve log** 체크박스 활성화
3. **페이지 새로고침**
4. **무한 새로고침 패턴 확인**
   - 같은 요청이 계속 반복되는지 확인
   - 어떤 파일이 계속 로드되는지 확인

---

## 🛠️ 일반적인 해결 방법

### 방법 1: 브라우저 캐시 완전 삭제

```bash
# Chrome 설정
1. 브라우저 설정 (⋮ > 설정)
2. "개인정보 및 보안" > "인터넷 사용 기록 삭제"
3. "전체 기간" 선택
4. "캐시된 이미지 및 파일" 체크
5. "데이터 삭제" 클릭
```

### 방법 2: 시크릿 모드로 테스트

```
1. Chrome: Ctrl+Shift+N (Windows) / Cmd+Shift+N (Mac)
2. http://localhost:3000 접속
3. 무한 새로고침 여부 확인
```

### 방법 3: Service Worker 제거

1. **개발자 도구** > **Application 탭**
2. **Service Workers** 메뉴 선택
3. **Unregister** 버튼 클릭
4. 페이지 새로고침

### 방법 4: Local Storage 삭제

1. **개발자 도구** > **Application 탭**
2. **Storage** > **Local Storage** > **http://localhost:3000**
3. 우클릭 > **Clear** 선택
4. **Session Storage**도 동일하게 삭제

---

## 🐛 코드 레벨 문제 (개발자용)

### 의심되는 원인

#### 1. _app.js의 스크롤 복원 로직

**위치**: `pages/_app.js:44-100`

**문제**: 스크롤 위치 복원 시 무한 루프 발생 가능

**임시 해결방법**: 스크롤 복원 기능 비활성화

```javascript
// pages/_app.js 에서 다음 부분을 주석 처리

// 스크롤 관리 - 브라우저 기본 동작 비활성화
useEffect(() => {
  if (typeof window === 'undefined') return;

  // 이 부분을 주석 처리
  /*
  if ('scrollRestoration' in window.history) {
    window.history.scrollRestoration = 'manual';
    console.log('브라우저 스크롤 복원 비활성화됨');
  }
  */

  // ... 나머지 스크롤 관련 코드도 주석 처리
}, []);
```

#### 2. index.js의 navigateToPage 함수

**위치**: `pages/index.js:77-138`

**문제**: 동일 페이지 리다이렉트 무한 루프

**확인 방법**:
```javascript
// 브라우저 콘솔에서 확인
console.log('Router pathname:', window.location.pathname);
console.log('Router asPath:', window.location.href);
```

#### 3. Next.js Fast Refresh 문제

**문제**: 개발 모드에서 HMR이 전체 새로고침을 유발

**임시 해결방법**:
```bash
# 개발 서버 재시작
# 터미널에서
cd /Users/byungjinyou/Desktop/kstarpick-server-backup/kstarpick
rm -rf .next
npm run dev
```

---

## 📊 수집할 정보

문제 해결을 위해 다음 정보를 수집해주세요:

### 1. 브라우저 콘솔 에러
```
예시:
- TypeError: Cannot read property 'x' of undefined
- ReferenceError: variable is not defined
- etc.
```

### 2. Network 요청 패턴
```
예시:
- GET / (document) - 반복 횟수: 10회
- GET /_next/static/... - 반복 횟수: 5회
```

### 3. 발생 조건
- [ ] 최초 진입 시에만 발생
- [ ] 새로고침 시에도 발생
- [ ] 특정 브라우저에서만 발생 (Chrome/Safari/Firefox)
- [ ] 시크릿 모드에서도 발생

---

## 🔧 빠른 수정 (Quick Fix)

가장 빠른 해결 방법:

### 옵션 1: 프로덕션 모드로 테스트

```bash
# 개발 서버 중지 후
npm run build
npm run start

# http://localhost:3000 접속
```

프로덕션 빌드에서는 Fast Refresh가 비활성화되어 문제가 해결될 수 있습니다.

### 옵션 2: 포트 변경

```bash
# package.json의 dev 스크립트 수정
"dev": "next dev -p 3001"

# 또는 직접 실행
npm run dev -- -p 3001

# http://localhost:3001 접속
```

---

## 📞 추가 도움

위 방법으로 해결되지 않으면:

1. 브라우저 콘솔 스크린샷
2. Network 탭 스크린샷
3. 발생 조건 상세 설명

을 준비해주세요.
